[build]
  command = "npm run build"
  publish = ".next"

# Runtime oficial do Next para Netlify: transforma as rotas de servidor em
# Netlify Functions v2, que é o formato exigido pelo Netlify Identity.
[[plugins]]
  package = "@netlify/plugin-nextjs"
